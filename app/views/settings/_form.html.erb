<%= form_for(@setting) do |f| %>
  <% if @setting.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@setting.errors.count, "error") %> prohibited this setting from being saved:</h2>

      <ul>
      <% @setting.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :lin_emmision %><br>
    <%= f.text_area :lin_emmision %>
  </div>
  <div class="field">
    <%= f.label :lin_supply %><br>
    <%= f.text_area :lin_supply %>
  </div>
  <div class="field">
    <%= f.label :lin_demand %><br>
    <%= f.text_area :lin_demand %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
